@model CarManagement.Services.VehicleBuilder

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>VehicleBuilder</h4>
    <hr />
    <dl class="dl-horizontal"></dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>

@*@model CarManagement.Core.Models.DTOs.EnrollmentDto

    @{
        ViewBag.Title = "Delete";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

    @using (Html.BeginForm("Delete", "Vehicles", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        <h2>Delete</h2>

        <h3>Are you sure you want to delete this?</h3>
        <div>
            <h4>EnrollmentDto</h4>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Serial)
                </dt>

                <dd>
                    @Html.TextBoxFor(Model => Model.Serial, new { @readonly = "readonly" })
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Number)
                </dt>

                <dd>
                    @Html.TextBoxFor(Model => Model.Number, new { @readonly = "readonly", @Value = Model.Number.ToString("0000") })
                </dd>

            </dl>

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>

        </div>
    }*@
